import React from 'react';

const Input = (label) => {
    return (
        <div className="input">
            <input />
            <label>{ label }</label>
        </div>
    )
}

export default Input;